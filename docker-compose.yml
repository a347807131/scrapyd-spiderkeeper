version: '2'

services:
  sdsk:
    image: a347807131/scrapyd-spiderkeeper
    build: .
    environment:
      - SD_SERVER=
      - SK_USERNAME=admin
      - SK_PASSWORD=admin
  mysql:
    extends:
      file: mysql.yml
      service: mysql

